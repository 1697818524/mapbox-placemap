# 启动说明

项目结构、环境准备和运行方式已经统一维护到根目录 `README.md`。

常用启动命令：

```powershell
cd D:\任务\JSJ\mapbox-placemap
npm run dev
```

默认访问地址：

- 前端：`http://127.0.0.1:3000/`
- 后端：`http://127.0.0.1:8000/`
- 后端 OpenAPI：`http://127.0.0.1:8000/docs`

接口说明见 `API.md`。以后接口字段、路径、请求体或响应结构有变化时，同步修改 `API.md`。

