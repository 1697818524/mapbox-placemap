# 地方感地图配色生成系统计划书

## 目标定位

本项目定位为一个地图配色工作台，而不是普通展示页。用户围绕同一条主流程完成操作：搜索地点、收集地点图片、运行视觉分析流水线、生成地图配色方案，并在 Mapbox 地图上实时预览结果。

## 页面规划

### 首页工作台

首页采用三栏结构：

- 左侧为地点与样本图片区。用户可以搜索地点、查看相关图片、勾选搜索图片，也可以上传本地图片。该区域承担样本集构建与 pipeline 启动入口。
- 中间为 Mapbox 地图主画布。地图保持常驻，所有配色变化都即时反馈到地图图层上。
- 右侧为地图图层配色面板。按水体、道路、建筑、绿地、标注等图层分类，支持手动调色、重置颜色，并在 pipeline 成功后生成候选方案。

### 方案生成页

`/generate` 页面继续使用三栏结构：

- 左侧为方案评分面板，展示语义匹配、可读性、多样性、和谐度、地方代表性等指标。
- 中间为地图预览区，切换方案时实时更新地图样式。
- 右侧为候选方案控制区，支持上一套、下一套、重新生成，并保留每个图层的颜色编辑能力。

## 交互流程

1. 用户搜索地点。
2. 前端调用图片搜索接口，展示地点相关图片。
3. 用户选择搜索图片或上传本地图片。
4. 前端将图片采集到后端，创建 pipeline job。
5. 后端依次执行图像增强、语义分割、超像素、语义赋值、调色板聚类、方案生成。
6. 前端轮询 job 状态，展示当前阶段与进度。
7. pipeline 成功后，生成按钮解锁。
8. 用户生成并切换候选配色，地图实时应用对应方案。

## 后端计划

- 图片模块负责搜索、代理展示、上传和 URL 采集。
- Pipeline 模块负责异步任务、阶段进度、产物登记和方案读取。
- 视觉处理模块包括阴影增强、OneFormer 语义分割、SLIC 超像素、五类语义映射和颜色聚类。
- 方案模块优先使用 pipeline 聚类出的语义调色板；无聚类结果时使用规则化 HSL 方案兜底。
- 优化模块使用 NSGA-II 评估和谐度与地方代表性，作为后续高级方案生成入口。

## 前端计划

- 使用 Vue 3、Pinia、Vue Router、Element Plus 和 Mapbox GL。
- `Home.vue` 作为主工作台入口。
- `SchemeGenerate.vue` 作为方案比较与再生成页面。
- `MapInfo.vue` 管理地点搜索、图片选择、上传、pipeline 启动和进度。
- `MapDisplay.vue` 维护地图主画布。
- `MapStyle.vue` 管理图层颜色、当前方案、候选方案切换和地图实时应用。
- `ObjectiveScoresPanel.vue` 展示候选方案评分。

## 当前完成范围

- 已建立前后端主流程接口。
- 已补齐 pipeline job、artifact、scheme 等核心数据模型。
- 已实现图片搜索、采集、上传、代理展示。
- 已实现工作台式三栏页面和方案生成页。
- 已实现地图图层配色编辑、候选方案切换、评分展示。
- 已通过前端类型检查、前端生产构建和后端 Python 语法检查。

## 后续增强

- 增加 pipeline 产物预览页，展示语义分割图、超像素图、聚类调色板。
- 增加方案收藏、导出 Mapbox style JSON。
- 增加任务历史列表，支持恢复已有 job。
- 增加大图处理队列和失败阶段重试。
- 优化前端 chunk 拆分，降低生产包体积提示。
